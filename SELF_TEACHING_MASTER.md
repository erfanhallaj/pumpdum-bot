# 🎓 Self-Teaching Master System

## معرفی

سیستم **Self-Teaching Master** یک معلم کامل و خودمختار است که دسترسی 100% به کد دارد و می‌تواند:
- ✅ کد را بخواند
- ✅ کد را تغییر دهد
- ✅ کد جدید بنویسد
- ✅ کد را حذف کند
- ✅ از بک‌تست‌ها یاد بگیرد
- ✅ مشکلات را شناسایی و حل کند
- ✅ خودش را به‌روزرسانی کند

## 🔧 قابلیت‌ها

### 1. تحلیل خودکار بک‌تست
- هر روز بک‌تست اجرا می‌شود
- مشکلات شناسایی می‌شوند:
  - Win rate پایین
  - Timeout زیاد
  - Loss زیاد
  - Profit منفی
  - سیگنال کم

### 2. تولید خودکار Fix
- از AI برای تولید کد استفاده می‌کند
- یا از rule-based fixes استفاده می‌کند
- Fix ها قبل از اعمال تست می‌شوند

### 3. اعمال خودکار تغییرات
- Backup خودکار قبل از تغییر
- تست قبل از اعمال
- Rollback خودکار در صورت خطا
- Verification بعد از اعمال

### 4. یادگیری مداوم
- تاریخچه تمام تغییرات
- یادگیری از مشکلات قبلی
- بهبود مداوم

## ⚙️ تنظیمات

در `config.py`:

```python
# فعال/غیرفعال کردن
ENABLE_SELF_TEACHING_MASTER = True  # فعال کردن

# فاصله زمانی
SELF_TEACHING_INTERVAL = 86400  # هر 24 ساعت

# امنیت
SELF_TEACHING_TEST_BEFORE_APPLY = True  # تست قبل از اعمال
SELF_TEACHING_AUTO_ROLLBACK = True  # Rollback در صورت خطا

# AI Configuration
SELF_TEACHING_USE_AI_CODE_GENERATION = True  # استفاده از AI
SELF_TEACHING_AI_PROVIDER = 'huggingface'  # 'huggingface', 'openai', 'local'
```

## 📁 فایل‌های قابل تغییر

سیستم می‌تواند این فایل‌ها را تغییر دهد:
- `config.py`
- `ai_analyzer.py`
- `backtester.py`
- `monitor.py`
- `strategy_optimizer.py`
- `coin_specific_learner.py`
- `pattern_learner.py`
- `adaptive_filter.py`
- `loss_analyzer.py`

## 🛡️ فایل‌های محافظت شده

این فایل‌ها **هرگز** تغییر نمی‌شوند:
- `self_teaching_master.py` (خود سیستم)
- `main.py` (نقطه ورود)
- `telegram_bot.py` (ارتباطات)
- `requirements.txt` (وابستگی‌ها)

## 🔄 فرآیند کار

1. **بک‌تست روزانه**: اجرای بک‌تست برای ارزیابی عملکرد
2. **شناسایی مشکلات**: تحلیل نتایج و پیدا کردن مشکلات
3. **تولید Fix**: تولید کد برای حل مشکلات
4. **تست Fix**: تست کردن fix قبل از اعمال
5. **اعمال تغییرات**: اعمال fix های موفق
6. **Verification**: بررسی بهبودها

## 📊 گزارش

گزارش‌ها در `models/teaching_history.json` ذخیره می‌شوند.

می‌توانید گزارش بگیرید:
```python
report = self_teaching_master.get_teaching_report(days=7)
print(report)
```

## ⚠️ هشدارها

1. **Backup**: همیشه backup بگیرید قبل از فعال کردن
2. **Test Mode**: ابتدا با `TEST_BEFORE_APPLY=True` تست کنید
3. **Monitoring**: تغییرات را بررسی کنید
4. **API Keys**: برای AI code generation نیاز به API key دارید

## 🚀 شروع سریع

1. فعال کردن در `config.py`:
```python
ENABLE_SELF_TEACHING_MASTER = True
```

2. ربات را اجرا کنید - سیستم خودکار شروع می‌شود

3. گزارش‌ها را در Telegram دریافت کنید

## 📝 مثال Fix

مثال: اگر win rate پایین باشد، سیستم خودکار:
1. `MIN_CONFIDENCE_SCORE` را افزایش می‌دهد
2. `MIN_AI_CONFIDENCE` را افزایش می‌دهد
3. تست می‌کند
4. اگر بهبود داشت، اعمال می‌کند

## 🔐 امنیت

- تمام تغییرات backup می‌شوند
- تست قبل از اعمال
- Rollback خودکار در صورت خطا
- فایل‌های مهم محافظت می‌شوند

