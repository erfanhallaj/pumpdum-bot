# 🔧 راهنمای رفع مشکل تلگرام

## مشکل: "Chat not found"

اگر این خطا را می‌بینید، یکی از این کارها را انجام دهید:

### روش 1: استفاده از Chat ID عددی (توصیه می‌شود)

1. به ربات [@userinfobot](https://t.me/userinfobot) در تلگرام پیام دهید
2. عددی که به شما می‌دهد را کپی کنید (مثلاً: `123456789`)
3. در فایل `.env` یا `config.py`، `TELEGRAM_CHAT_ID` را به این عدد تغییر دهید

### روش 2: استفاده از Username

اگر می‌خواهید از username استفاده کنید:

1. مطمئن شوید که به کانال/گروه `@pythontrade_ai` دسترسی دارید
2. مطمئن شوید که ربات را در آن کانال/گروه اضافه کرده‌اید
3. به ربات یک پیام بفرستید (در کانال یا به صورت خصوصی)
4. در فایل `.env` یا `config.py`، `TELEGRAM_CHAT_ID` را به `@pythontrade_ai` تغییر دهید

### بررسی تنظیمات

فایل `config.py` را باز کنید و خط 11 را بررسی کنید:

```python
TELEGRAM_CHAT_ID = os.getenv('TELEGRAM_CHAT_ID', '@pythontrade_ai')
```

یا فایل `.env` را ویرایش کنید:

```
TELEGRAM_CHAT_ID=@pythontrade_ai
```

یا اگر Chat ID عددی دارید:

```
TELEGRAM_CHAT_ID=123456789
```

### نکته مهم

- اگر از username استفاده می‌کنید، باید با `@` شروع شود
- اگر از Chat ID عددی استفاده می‌کنید، نباید `@` داشته باشد
- مطمئن شوید که ربات را استارت کرده‌اید (به [@BotFather](https://t.me/botfather) پیام `/start` بدهید)

