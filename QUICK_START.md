# 🚀 راهنمای سریع اجرای ربات

## ✅ ربات در حال اجرا است!

ربات AI Pump Detection در پس‌زمینه در حال اجرا است.

## 📊 وضعیت ربات:

ربات در حال انجام این کارها است:
1. ✅ اتصال به صرافی‌ها (Binance, KuCoin, Gate, MEXC)
2. 🔄 پیدا کردن کوین‌های کوچک (Small Cap)
3. 🔄 جمع‌آوری داده‌های تاریخی
4. 🔄 آموزش مدل‌های AI
5. 🔄 شروع مانیتورینگ لحظه‌ای

## 📱 بررسی وضعیت:

### روش 1: بررسی در تلگرام
- به کانال/گروه `@pythontrade_ai` بروید
- ربات وضعیت را به تلگرام ارسال می‌کند

### روش 2: بررسی در کنسول
- پنجره PowerShell/CMD را باز کنید
- خروجی ربات را مشاهده کنید

## 🛑 توقف ربات:

برای توقف ربات:
- در پنجره کنسول: `Ctrl + C` بزنید
- یا پنجره را ببندید

## 🔄 راه‌اندازی مجدد:

برای راه‌اندازی مجدد:
```bash
python main.py
```

یا:
```bash
python run_bot.py
```

یا در ویندوز:
```bash
start.bat
```

## ⚙️ تنظیمات سریع:

برای تغییر تنظیمات، فایل `config.py` را ویرایش کنید:

```python
# برای شروع سریع‌تر:
SKIP_INITIAL_TRAINING = True  # Skip آموزش اولیه
SKIP_INITIAL_BACKTEST = True   # Skip بک‌تست اولیه

# تعداد کوین‌ها:
MAX_COINS_TO_MONITOR = 50  # کمتر = سریع‌تر
```

## 📈 انتظارات:

- **زمان شروع**: 2-5 دقیقه
- **اولین هشدار**: ممکن است 10-30 دقیقه طول بکشد
- **به‌روزرسانی مدل**: هر 1 ساعت
- **بهینه‌سازی**: هر 24 ساعت

## 🆘 مشکل دارید؟

اگر ربات کار نمی‌کند:
1. بررسی کنید که Python نصب است: `python --version`
2. پکیج‌ها را نصب کنید: `pip install -r requirements.txt`
3. فایل `.env` را بررسی کنید
4. لاگ‌ها را بررسی کنید

---

**موفق باشید! 🚀**

